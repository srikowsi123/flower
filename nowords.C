#include<stdio.h>
int main()
{
    char a[20];
    int i,words;
    printf("enter the string");
    for(i=0;i<n)
    words=1;
    while(a[i]!='\n')
    {
    	if(a[i]==' '||a[i]=='\n'||a[i]=='\t')
        {
            words++;
        }

        i++;
    }
	printf("total no.of words: %d",words);
	return 0;
}
